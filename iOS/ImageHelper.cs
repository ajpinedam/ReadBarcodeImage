using System;
using System.Drawing;

namespace readBarcode.iOS
{
	public class ImageHelper
	{
		public ImageHelper()
		{
		}


		Bitmap GetImageBitMap(string fileName) 
		{ 
			var bmp = new System.Drawing.Bitmap(fullName);
		}
	}
}
